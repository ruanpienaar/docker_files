# Kafka Example cluster

This is a example kafka cluster with
1 zookeeper server
3 kafka brokers

deploy the stack:
```
$ ./deploy_stack.sh
```

remove the stack ( stop it )
```
$ ./remove_stack.sh
```