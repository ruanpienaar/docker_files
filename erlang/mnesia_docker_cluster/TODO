
 ☐ log the contents of the vm, so docker logs, can inspect.
 ☐ be able to add more nodes on the fly
 ☐ if more nodes are added, then the sys.config needs regen mount the mnesia dir's in a Volume on the host?
 ☐ Make a example to READ and WRITE data on the cluster?
 ☐ Maybe clone outside of Docker, and use ADD/COPY to add the mnesia_docker code.